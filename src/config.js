export const ROOT_DOMAIN = "naowalrahman.com";

// Canonical origin, no trailing slash. Every indexable URL is built from this
// so the sitemap, the <link rel="canonical"> tags, and the deployed CNAME all
// agree on one host.
export const SITE_URL = `https://${ROOT_DOMAIN}`;

export const SITE_NAME = "Naowal's Corner";

export const SITE_DESCRIPTION =
    "Naowal Rahman — CS & applied math student at Columbia, ML researcher, and avid Linux user. Projects, writing, and more.";
